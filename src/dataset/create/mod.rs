use crate::{Dataset, Result};
use std::path::PathBuf;

pub async fn create_dataset(dataset: &Dataset, name: &str) -> Result<()> {
    // check that path/name doesn't exist
    // create directory name
    // write .csvs.csv
    // write schema
    Ok(())
}
